# FlowDesk

Active continuation: finish Windows persistent desktop message listening and continuous multi-conversation replies, with automatic and manual-copy modes. Implement ordinary window support; development verification remains confined to FlowDesk-owned fictional desktop chat windows. Do not describe the previous single-task build as a finished commercial product.

Independent implementation informed by the public Thiflow product page. Build a local-first Chinese marketing website and Windows assistant. No upstream code, branding, customer data, or commercial claims.

Current direction (reconfirmed after the 0.2 package): Windows software + customer-configured visual model + knowledge/workflows + USB HID execution, with automatic-reply and manual-copy modes. Reuse the existing software and Pico SDK/TinyUSB firmware. As confirmed on 2026-09-15, the supported customer and acceptance scope is Windows 11 x64 only; Windows 7/8/8.1/10, Windows ARM64 and Mac are outside the current scope. Complete Windows real-machine acceptance and fix discovered issues first; the intended next stage is to sell the accepted Windows version while developing Mac. Do not start Mac development or imply commercial acceptance before that milestone. The user has purchased a Pico H and is awaiting delivery. The implemented firmware targets Raspberry Pi Pico 1 (RP2040, non-W), not Leonardo/ATmega32u4. Do not assume a competitor's finished USB device supports FlowDesk's protocol.

Current implementation boundary: source version 0.7.0 adds Google official `@google/genai@2.22.0` native Interactions desktop Computer Use and Gemini native persistent multi-conversation replies, while retaining the ordinary OpenAI-compatible vision path. Gemini uses a Gemini API Key, not Antigravity OAuth; model names remain account-dependent. Persistent replies may select Gemini native or ordinary vision. Matching FlowDesk PnP Pico USB HID is required, but automatic discovery never starts a task; management and flashing instructions remain available offline. Pico remains firmware 0.5.0 on protocol 4, so an already matching board does not need reflashing for the 0.7 software update. Knowledge/SOP learning produces pending candidates only; local organization and explicitly authorized model organization both require human approval before enabling. Attachments are only explicitly imported PNG/JPEG/WebP, MP3/WAV, PDF/CSV/TXT; chat attachments are not auto-collected. USB Chinese input remains per-key ASCII full pinyin with candidate selection; it does not use clipboard, paste or Enter. The user authorized development and verification using FlowDesk's own fictional-message windows, virtual USB, fake HAL tests and loopback model stubs. Physical board, real Windows HID/input-method integration, real model/account quality and customer acceptance remain pending. Never call a real model or operate real accounts as part of development checks. Consult docs/Windows-0.7-验收记录.md and the actually verified package receipt rather than assuming every source change is packaged.

Use React + TypeScript + Vite, a shared local service, and an isolated Electron renderer. Autonomous replies are a core product requirement. The user explicitly authorized automatic replies and sends within a dedicated local HTML simulator using fictional data. Keep that test environment separate from real accounts and production records. Real-account actions still require explicit authorization for the target account and scope. Demonstration content must be labeled. Never fabricate live model results, sends, or integrations. API keys are never part of exported state. Desktop secrets use Electron safeStorage; browser server secrets are memory-only or environment supplied.

Validation for this round: build, existing tests, self-hosted web checks, and `package:win` with isolated temporary user data. Native runtime checks must be limited to FlowDesk itself and its own fictional-message simulator. Never capture, read, type into, or send to WeChat or other real accounts. Report packaged startup, simulator checks, and the user's second-computer acceptance separately. Keep scope single-user local; no billing or multi-tenant claims.
